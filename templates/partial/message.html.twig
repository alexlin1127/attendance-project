{% extends 'dashboard.html.twig' %}

{% block content %}
<div class="row row-cols-md-1 mt-5 mb-3">
    <div class="col">
        <div class="alert {{alert_type}} alert-dismissible fade show" role="alert">
            <div>{{message | raw}}</div>
            <a type="button" href="att_list.php" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></a>
        </div>
    </div>
</div>
{% endblock %}
